saved_queries:
  - name: test_saved_query
    description: "Testing"
    label: test
    config:
      cache:
        enabled: true
    query_params:
      metrics:
        - total_count_patient
    exports:
      - name: total_count_test
        config:
          export_as: table